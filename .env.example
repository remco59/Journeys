# Copy to .env for local docker compose use.
#
# For a production deploy (compose.prod.yml — see that file's header
# comment), also set:
#   APPDATA_PATH   — host path for persistent data (Unraid: /mnt/user/appdata/journeys)
#   WEB_PORT       — host port to publish the app on (default 3000)
# and use a real POSTGRES_PASSWORD / BOOTSTRAP_ADMIN_PASSWORD — the
# defaults below are for local development only.

# Postgres
POSTGRES_DB=journeys
POSTGRES_USER=journeys
POSTGRES_PASSWORD=journeys_dev_password
DATABASE_URL=postgres://journeys:journeys_dev_password@postgres:5432/journeys

# Storage
STORAGE_DRIVER=local
LOCAL_STORAGE_PATH=/data/photos

# S3 (only used when STORAGE_DRIVER=s3)
S3_ENDPOINT=
S3_REGION=
S3_BUCKET=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=

# Auth
SESSION_COOKIE_NAME=journeys_session
SESSION_TTL_DAYS=30

# Bootstrap admin (used once, on first boot, if no admin user exists yet)
BOOTSTRAP_ADMIN_USERNAME=admin
BOOTSTRAP_ADMIN_PASSWORD=change-me-now

# Geocoding
GEOCODE_PROVIDER=photon
GEOCODE_BASE_URL=https://photon.komoot.io

# Rail route lookup (train trace snapping) — free, no key needed
OVERPASS_BASE_URL=https://overpass-api.de/api/interpreter

# Road route lookup (car/cycling/walking trace snapping) — free, no key needed
OSRM_CAR_BASE_URL=https://routing.openstreetmap.de/routed-car
OSRM_BIKE_BASE_URL=https://routing.openstreetmap.de/routed-bike
OSRM_FOOT_BASE_URL=https://routing.openstreetmap.de/routed-foot

# Map tiles — CARTO Positron: a cleaner, less cluttered basemap than stock OSM
TILE_PROVIDER_URL=https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png
TILE_ATTRIBUTION=© OpenStreetMap contributors © CARTO
